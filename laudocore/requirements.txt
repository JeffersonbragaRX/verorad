# LaudoCore — dependencias Python (vertical piloto, Fase 0-8).
# Fases 0-4 usam so a stdlib. A partir da API (Fase 6/8):
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5

# Apenas para rodar os testes da API (tests/test_api.py):
httpx==0.28.1
